query Parts($filter: PartsFilterInput) {
  parts(filter: $filter) {
    items {
      id
      name
      category
      brand
      model
      condition
      description
      price
      currency
      oemCode
      compatibility
      stock
      location
      imageUrl
    }
    total
  }
}
